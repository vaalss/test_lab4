# Laboratorio 4

`Proyecto desarollado con JavaFX, Maven y SceneBuilder`
`Correr con comando: mvn clean javafx:run`
 
 # Requerimientos:
- JDK (al menos version 24) (utilizamos la versión 24.0.1)
- JavaFX (recomendable utilizar version 21 por problemas de compatibilidad con versiones más actuales) (utilisamos la versión 21. 0.9)
- Maven (âl menos version 3.6) (utilizamos la versión 3.9.11)

# Recomendaciones:
- Instalar extensiones "Java Extension Pack" y "Project Manager for Java""# test_lab4" 
